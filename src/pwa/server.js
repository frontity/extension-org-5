import Store from '../shared/stores/server';
import env from '../shared/env';
import { version } from '../../package.json';

const ExtensionOrg5 = () => null;

export default ExtensionOrg5;
export { Store, env, version };
